#include <stdio.h>
int main() {
    int i = srand( time( NULL ) );
    if ( i < 2 ) {
        printf( "ahahahha, i'm the dirtest dirt\n" );
    }
    else {
        printf( "the gunk in the gutter\n" );
    }
}